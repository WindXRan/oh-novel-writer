---
name: story-rewrite
description: |
  结构仿写 · 套用爆款骨架写新书。提取爆款小说的深层叙事结构，用全新角色和背景填充。
  触发方式：/story-rewrite、/仿写、/结构仿写、「仿写这本书」「套用这个骨架写新书」
  批量模式触发：「批量写」「批量仿写」「一次性写完」「批量{数字}章」
---

# story-rewrite：结构仿写

**核心信念：只仿"骨相"，不仿"皮相"。**

**架构原则（分角色）：**
- **Phase 1（主线程）**：读源文本 + narrator-persona.md，提取框架和语感样本
- **Phase 2（章纲）**：`story-architect` subagent，自带钩子/反转/情绪弧线知识
- **Phase 3（正文）**：`narrative-writer` subagent，自带场景展开/6 Gate去AI/节奏规则。
  Phase 3 agent **可读源文本前 3 章学风格**（不学情节），章纲已锁定事件，不会抄。
- **主线程不做** narrative-writer.md 的预提取——agent 按需自读参考文件。

---

## ⚠️ 核心约束

### 反调色盘（防止"扒骨肉"）

仿写必须在以下3个维度上与源文本**完全不同**：

| 维度 | 说明 |
|------|------|
| **开篇方式** | 如源文本用心理治疗/催眠开篇，新书必须用其他方式（商战/遗物/急救等） |
| **破冰方式** | 如源文本用送物被拒，新书必须用其他方式（替解围/共同任务/意外接触等） |
| **节奏安排** | 如源文本一章写重生前，新书必须压缩或改变节奏 |

### 时间线连续

Phase 1 框架只提供「情绪弧线+叙事功能」，不提供具体场景。Phase 2 生成章纲锁定关键事件。Phase 3 agent 在章纲内自由发挥。

---

## 流程总览

### 试水模式（3章）

```
Phase 1（主线程，~2min）  Phase 2（1 agent，~2min）  Phase 3（3 agent 并行，~3min）  Phase 4（主线程，~30s）
读源文本 + 提取框架        生成章纲（每章情节点）        仿写3章（prompt即写，不读文件）   合并TXT + 字数统计 + 自检
```

**总耗时：~7-8分钟（试水3章）**

### 批量模式（全书）

```
Phase 1（一次性）: 全书章纲生成 → 存储到文件
Phase 2（循环）: 3章并行写 → 质量检查 → 不达标重写 → 合并 → 摘要压缩 → 滚动
Phase 3（结尾）: 全局一致性检查 + 禁用词扫描
```

**20章耗时：约15-20分钟，一次完成**

---

## 批量模式入口

用户输入：`/story-rewrite 批量 {源文本路径} {目标章数}`

示例：`/story-rewrite 批量 仿写试水库/源文本.txt 20`

**主流程**：

```
1. Phase 1: 读取源文本 + 生成仿写框架 + 全书章纲（一次性）
2. Phase 2: 分批写作循环（每批3章）
   loop:
     读取下3章章纲
     并行启动3个agent写作
     每章质量检查（禁用词+字数+钩子）
     不达标则重写（最多2次）
     合并到正文目录
     压缩前文摘要（近6章详记+更早概要）
     if 已完成目标章数 → 结束循环
     else → 继续循环
3. Phase 3: 全书质量检查
```

---

## Phase 1：结构提取 + 预加载

**用户只提供源文本路径，剩下全部自主完成。**

主线程收到源文本后，**同步完成以下操作**（不启动 agent，直接在主线程做）：

### Step 1：读取源文本

读取前3章 + 最后1章，记录：
- 源书名、题材、章数
- 开篇方式（具体场景 → 抽象为叙事功能）
- 破冰方式（具体事件 → 抽象为叙事功能）
- 节奏安排（各章情绪基调）
- 人设标签（女主/男主/配角/反派）
- 结局走向
- **叙述者风格**（关键，决定仿写是否"像人"）：
  1. 叙述者是否跳出来评论（有→吐槽型/冷幽默型/故事人型；无→沉浸型/冷峻型）
  2. 是否有现代人视角/网感语言（强→吐槽型；中→冷幽默/沉浸型；弱→故事人/冷峻型）
  3. 句子平均长度（<15字→吐槽型/冷峻型；15-25字→冷幽默/沉浸型；>25字→故事人型）
  4. 是否有第四面墙碎裂（有→吐槽型；偶尔→沉浸型；无→其他）
  5. 情绪外放程度（高→吐槽型；中→沉浸型；低→冷幽默/冷峻型）
  6. 口语化程度（极高→吐槽型/故事人型；中→冷幽默/沉浸型；低→冷峻型）
   7. **从源文本前3章提取 3-5 个核心节奏特征**（不是具体句子，是选词和节奏倾向），作为仿写的"语感特征"。每个特征用一句话描述 + 一个简短示例（不超过10字）。示例：
      - 信息直给：不铺垫，第一句直接扔事实（"再睁眼，入目是一盏水晶吊灯"）
      - 段落极短：1-3句一段，没有4句以上的段落
      - 对话省略标签：超过70%的对话不写"他说""她说"
      - 跳过过渡：不写"她站起来走了出去"，直接切到下一场景
      - 吐槽打断叙事：吐槽出现在段落中间打断叙事，不在段落末尾当补丁
   8. **从源文本前3章提取 3-5 段语感样本**（原文片段，每段 30-100 字），供 Phase 3 narrative-writer 做风格参照。选标准：
      - 选最有辨识度的段落（读者一眼能认出"这是这本书的味道"）
      - 涵盖不同场景类型（对话/内心/动作/描写各 1 段）
      - 不选情节推动段落（如高潮反转），选日常段落（能体现 baseline 语感）
      - 每段标注风格标签（如"内心独白型""吐槽打断型""对话省略标签型"）

### Step 2：参考文件准备

Phase 3 使用 `narrative-writer` subagent，它自带参考文件体系（按需读取），主线程**不需要**预提取 narrative-writer.md。

主线程只准备以下内容：

1. **banned-words 摘要**（可选）：从 `{项目绝对路径}/skills/story-deslop/references/banned-words.md` 提取**一级禁用词 20 词以内**，仅用作 Phase 4 写后扫描，**不注入 Phase 3 prompt**
2. **人格定义**：从 `{项目绝对路径}/skills/story-rewrite/references/narrator-persona.md` 根据 Step 1 分析匹配最接近的人格类型，**保留完整定义文字**（"你是谁"块需要完整人格描述），用于 Phase 3 prompt 的`{人格完整定义}`占位符。如果源文本风格介于两种人格之间，允许混合

⚠️ **权限限制**：`Glob` 搜索范围必须限制在项目目录内，直接拼接项目绝对路径 `Read`。

### Step 3：生成仿写框架

输出以下框架（只保留抽象模式，禁止具体场景）：

```
- 新书名：
- 新主角：（名字+核心标签，必须与源文本不同）
- 新背景：（时代/世界观，必须与源文本不同时代/不同地域）
- 核心冲突：（1句话）
- 开篇叙事功能：（抽象，如「铺垫→创伤回忆→重生契机」）
- 破冰叙事功能：（抽象，如「主动靠近→遭遇冷遇→坚持」）
- 开篇差异点：（必须与源文本不同）
- 破冰差异点：（必须与源文本不同）
- 节奏对齐表（3章）：
  - 第1章：情绪基调 / 叙事功能 / 弧线走向
  - 第2章：情绪基调 / 叙事功能 / 弧线走向
  - 第3章：情绪基调 / 叙事功能 / 弧线走向
- 人物关系模式（抽象）：
- 信息差设计：
- 核心差异对照表（时代/地域/职业/身份层面）：
- 角色名表（主线程必须统一，禁止 Agent 自行改名）：
  - 女主：{名字}
  - 男主：{名字}
  - 配角A：{名字+功能}
  - 配角B：{名字+功能}
- 题材标签：
- **叙述者人格**：（从 narrator-persona.md 匹配的人格类型名称 + 完整定义文字）
- **叙述者语感特征**：（从源文本提取的 3-5 个核心节奏特征，每个是一句话描述+简短示例，不是具体句子。示例见 Step 1.7）
- **叙述者语感样本**：（Phase 1 Step 1.8 提取的 3-5 段原文片段，每段 30-100 字 + 风格标签）

## 关键事件锚点表（Phase 1 主线程填写，Phase 2 章纲展开，Phase 3 agent执行）
- 第1章锚点：{必须发生的事件A}，{必须发生的事件B}，本章以{悬念/情绪点}收尾
- 第2章锚点：时间线{紧接第1章/次日/数日后}，{必须发生的事件A}，{必须发生的事件B}
- 第3章锚点：时间线{紧接第2章/次日/数日后}，{必须发生的事件A}，{必须发生的事件B}
```

**自检**：框架给没读过源文本的人，能据此写出完全不同但结构相似的故事吗？不能 → 重新抽象化。**叙述者语感特征是否已填入框架？** 没有 → 回到 Step 1 补充提取。

---

## Phase 2：章纲生成

### 试水模式：生成3章章纲

框架生成后，启动 1 个 `task(subagent_type: "story-architect")` 生成章纲。

### 批量模式：一次性生成全书章纲

**关键优化**：批量模式下，一次性生成全书所有章的章纲，存储到文件 `{书名}/大纲/章纲_全书.md`，后续按需读取。

**章纲生成 prompt（试水/批量通用）**：

```
你是故事架构师（story-architect）。任务：基于仿写框架，生成{目标章数}章的章纲。

你拥有以下参考文件，按需读取：
- `story-long-write/references/hooks-chapter.md`（章首7式+章尾13式）
- `story-long-write/references/hooks-suspense.md`（期待感管理、三翻四震）
- `story-long-write/references/reversal-toolkit.md`（5种反转类型+嵌套反转）
- `story-long-write/references/emotional-arc-design.md`（6种弧线速查）
- `story-long-write/references/outline-conflict.md`（AB交织法、高潮逆推）
- `story-long-write/references/outline-rhythm.md`（升级感三步设计法）

## 章纲格式要求
每章输出以下内容：
- 章名：{章名}
- 时间线：{本章发生在什么时间}
- 核心事件：{一句话概括本章}
- 情节点序列：3-5个情节点，每个写清"谁做了什么"（约200-300字/个情节点）
  ⚠️ 信息密集情节点（记忆导入/闪回/世界观交代）禁止用"记忆涌入""了解了背景"等一句概括。
  必须拆成"触发→信息→回到当下"微循环，每个微循环写清触发方式和交付的信息内容。
  详见硬约束第9条。
- 目标情绪：{本章交付什么情绪}
- **章首钩子**：利用钩子章法设计（冲突前置/信息差钩/反常行为/悬念句等），具体到一句话
- **爽点**：{具体爽点}
- **章尾钩子**：参考章尾13式设计（突然揭示/紧急危机/两难抉择等），写具体内容 + 期待度（强/中/弱）
- 字数目标：{X} 字

## 硬约束
1. 章纲必须覆盖框架中的「关键事件锚点表」（前3章）
2. 后续章节必须延续前3章的情绪弧线和叙事功能
3. 章节之间时间线必须自然衔接
4. 禁止写具体场景描写，只写事件摘要
5. 角色名必须使用框架「角色名表」中的名字
6. 每章字数目标：2000字（通过范围 1600-2400）
7. 每3-4章必须有一个小高潮或转折点，参考反转工具箱设计
8. 每章章尾必须有钩子（强/中/弱）
9. **信息交付原则**：涉及信息密集的情节点（记忆导入/背景交代/世界观说明/过去闪回），
   章纲必须写明信息分几波交付，每波的触发方式（感官/物件/对话触发）和交付内容。
   禁止用"记忆涌入""了解了背景""知道了真相"等单句概括整块信息。
   这不是添加更多场景描写——是改变信息的交付节奏：信息不连续倒出，而是穿插在当下的动作和感受中逐波释放。
10. **期待感管理**：参考期待感管理六法则，确保每章结束时有续读动力

## 情绪弧线模板（全书）
- 第1-3章：开篇铺垫，建立核心冲突
- 第4-6章：发展深化，矛盾升级
- 第7-9章：第一次高潮，关键转折
- 第10-12章：新矛盾出现，关系变化
- 第13-15章：第二次高潮，真相浮现
- 第16-18章：最终对决，矛盾解决
- 第19-20章：收尾，情感收束

## 仿写框架
{框架内容}

返回格式示例：
### 第1章
- 章名：
- 时间线：
- 核心事件：
- 情节点序列：
  1. {谁做了什么}
  2. {谁做了什么}
  3. {谁做了什么}
- 目标情绪：
- 章首钩子：
- 爽点：
- 章尾钩子：{期待度：强/中/弱}
- 字数目标：2000字

（依此类推，直到第{目标章数}章）

⚠️ 信息密集情节点展开示例：
   ❌ "记忆涌入，了解身世和处境"
   ✓ "触觉触发第1波（被包养+退学）→皮肤贴合处发烫，拉回当下→
       呼吸喷在颈侧触发第2波（傅砚深身份+失忆+白月光）→她僵住，不敢动→
       恐惧触发第3波（三个月后精神病院结局）→她猛地弹开"
```

### 主线程提取章纲

agent返回后，主线程提取每章的：
- 章名
- 时间线
- 核心事件
- 情节点序列
- 目标情绪
- 章首钩子
- 爽点
- 章尾钩子（含期待度）
- 字数目标

这些内容将填入Phase 3 agent prompt中的「本章章纲」部分。

**批量模式**：章纲存储到 `{书名}/大纲/章纲_全书.md`，后续每批写作时按需读取对应章节的章纲。

---

## Phase 3：并行仿写

### 试水模式：3章并行

框架生成后，启动 3 个 `task(subagent_type: "narrative-writer")` 并行。

### 批量模式：3章并行

**关键优化**：narrative-writer 自带参考文件体系（场景展开法、6 Gate去AI、核心节奏），按需自读。
主线程只提供 chapter outline + 人格定义 + 语感样本，不预提取写作规则。

每个 agent 的 prompt 由以下模板生成：

```
# ⚠️ 叙事视角：必须第三人称

**长篇网文必须用第三人称叙述（角色名/"她/他"），绝对不能用第一人称（"我/我们"）。**
违反即重写。

---

## 你是谁（最重要，决定全文风格）

你不是"网文写作专家"。
你是{人格类型名称}。

{人格完整定义}

你的语言样本（模仿这种感觉，不是模仿具体句子）：
{语感样本1：原文片段 + 风格标签}
{语感样本2：原文片段 + 风格标签}
{语感样本3：原文片段 + 风格标签}

⚠️ 写每一句话之前，先问自己：这个{人格名称}会怎么讲这件事？

---

## 任务

基于章纲，原创第{N}章。

## 风格参照

源文本位于：{源文本路径}
前几章已标注语感特征，可供参考（读前3章学风格，不学情节）。
章纲已锁定事件，风格学习不会导致抄袭。

本章语感特征：
{特征1}
{特征2}
{特征3}

## 角色锚点
- 男主：{角色名表中的男主名}
- 女主：{角色名表中的女主名}
- 地名：{框架中的地名}
- 时代/地域：{框架中的时代地域}

## 时间线与前情
本章是第{N}章，时间线：{章纲中的时间线}
{前情摘要}

## 本章章纲
- 章名：{章名}
- 核心事件：{章纲中的核心事件}
- 情节点序列：{章纲中的情节点序列}
- 章首钩子：{章纲中的章首钩子}
- 爽点：{章纲中的爽点}
- 章尾钩子：{章纲中的章尾钩子}
- 字数目标：2000字

## 格式
- 第一行写：`第{N}章 {章名}`（独占一行）
- 返回正文即可，不要额外注释
```

**narrative-writer 会自动**（不需要在主线程约束）：
- 应用场景展开法（每章限1次完整三层展开）
- 执行核心节奏规则（动作不解释、内心独白砍半、跳过一个环节、每段换节奏）
- 执行6 Gate去AI流程（禁用词、句式打碎、心理外化、节奏打碎、去腔调、去升华）
- 控制字数达标
- 如果语感样本包含吐槽型/冷幽默型人格，叙事中会自然带出人格特征

---

### ⚠️ 关键事件锚定规则（防止多agent各自为政）

**问题根因**：多agent并行时，如果prompt没有锁定关键事件，每个agent会自行设计，导致章节间时间线断裂、事件版本冲突、人物行为矛盾。

**解法**：
1. Phase 1 框架包含「关键事件锚点表」，主线程在生成框架时把每章必须发生的1-2个关键事件锁死
2. Phase 2 生成章纲，将锚点展开为具体情节点（时间线+事件+钩子）
3. Phase 3 agent prompt 注入章纲内容，agent在章纲内自由发挥

锚点是「叙事功能+关键事件」层面的抽象描述，章纲是具体的情节点，agent在章纲框架内自行设计场景。

### 主线程构建 prompt 时的变量填充

| 变量 | 说明 |
|------|------|
| `{N}` | 章节号（1/2/3...） |
| `{总章数}` | 目标章数 |
| `{前情摘要}` | 批量模式：近6章详记+更早概要；试水模式：逐章递增 |
| `{人格类型名称}` | Phase 1 匹配的人格类型名称（如"吐槽型网文读者"） |
| `{人格完整定义}` | Phase 1 从 narrator-persona.md 提取的完整人格定义文字 |
| `{语感样本1/2/3}` | Phase 1 Step 1.8 从源文本提取的原文片段 + 风格标签 |
| `{特征1/2/3}` | Phase 1 Step 1.7 从源文本提取的核心节奏特征 |
| `{章纲内容}` | Phase 2 输出的当前章节章纲（章名+核心事件+情节点序列+目标情绪+章首钩子+爽点+章尾钩子+字数目标） |
| `{源文本路径}` | 源文本文件的完整路径，供 narrative-writer 读前3章学风格 |

---

## Phase 4：合并输出

Phase 3 的 agent 返回后，主线程执行以下步骤：

### Step 0：字数校验（主线程全权负责）

**agent 不校验字数，主线程兜底。** 超标截断，不足警告。

```powershell
# 字数统计（汉字+标点+英文字母，排除空格换行）
(Get-Content '文件路径' -Raw -Encoding UTF8) -replace '[\s]','' | Measure-Object -Character | Select-Object -ExpandProperty Characters
```

对每章正文执行：
1. 统计字数
2. 如果 > 2400：按段落边界从末尾截断，保留前 2400 字（截断到最近的完整段落）
3. 如果 < 1600：在终端警告「第N章字数不足，需补充」
4. 截断后的正文才写入最终文件

### Step 1：读取临时文件

从 `temp_{书名}_ch{N}.txt` 读取各章正文。

⚠️ 临时文件命名规则：`temp_{书名}_ch{1,2,3}.txt`，书名从框架中取，去掉特殊字符（空格保留为下划线）。合并完成后删除临时文件。

### Step X：文字质感快速自查（写入前必过）

**目的：确保读起来好看，不是机械执行规则。** 以下 4 项不达标说明读感有问题，点对点修：

| 检查项 | 好文字的标准 | 读感差时怎么办 |
|--------|-------------|---------------|
| 内心独白太长 | 角色想事情不超过 2 句，第 3 句切动作/对话 | 砍掉后半截推理，让读者自己补 |
| 情绪词反复出现 | "心里咯噔/心跳漏了一拍/愣住了/僵住了" 全文 < 2 次 | 换动作展示或直接删 |
| 对话标签太密 | 看起来"他说/她说"一个接一个 | 去掉明显不用标的，或用动作替 |
| 段落太匀称 | 长短段混着来，不全是三五行一段 | 找连续同长度段，拆或并，制造落差 |

### Step Y：结构特征扫描（写入前可选，仅试水模式初次验证时做）

**目的**：确认 narrative-writer 输出没有系统性 AI 结构指纹。只做统计扫描，不做修改。

```powershell
# 4项快速扫描（用 PowerShell 跑）
$content = Get-Content 'temp_{书名}_ch{N}.txt' -Encoding UTF8 -Raw

# 1. 内心独白超长检测（≥3个长推理链→警告）
$inner = [regex]::Matches($content, '[。！？][^。！？]{10,}(想|觉得|知道|明白)[^。！？]{10,}[。！？]')
if ($inner.Count -ge 3) { Write-Warning "内心独白链过长：$($inner.Count)处" }

# 2. 情绪模板词
$emotion = "心里咯噔|心跳漏了一拍|愣住了|僵住了|大脑空白|心中一惊"
$matches = [regex]::Matches($content, $emotion)
if ($matches.Count -ge 2) { Write-Warning "情绪模板词重复：$($matches.Count)次" }

# 3. 段落长度标准差（<30 → 可能太均匀）
$paragraphs = ($content -split '\r?\n\r?\n') | Where-Object { $_ -ne '' }
$lengths = $paragraphs | ForEach-Object { ($_ -replace '\s+','').Length }
if ($lengths.Count -ge 3) {
    $avg = ($lengths | Measure-Object -Average).Average
    $variance = ($lengths | ForEach-Object { [math]::Pow($_ - $avg, 2) } | Measure-Object -Average).Average
    $std = [math]::Sqrt($variance)
    if ($std -lt 30) { Write-Warning "段落标准差仅 $std（目标≥30）" }
}

# 4. 对话标签密度（>3/100字→可能太规律）
$tags = [regex]::Matches($content, '(他说|她说|他问|她问)').Count
$density = [math]::Round($tags / ($content.Length / 100), 1)
if ($density -gt 3) { Write-Warning "对话标签密度 $density/100字" }
```

**触发规则**：
- 试水模式首次运行时执行，观察 narrative-writer 输出质量
- 如有警告 → 不需要立刻修改，记录到终端，后续在 persona 定义中微调
- 无警告 → 人格注入生效，后续批次可以跳过此扫描

### 试水模式：合并为TXT

写入 `仿写试水库/试水_{新书名}.txt`，格式：

```
========================================
{新书名}
========================================
作者：AI仿写
题材：{题材}

【简介】
{简介}

========================================
【仿写框架】（供 /story-long-write 开书时提取）
========================================
{Phase 1 输出的完整框架}

========================================
第1章 {章名}
========================================
{正文}

----------------------------------------
第2章 {章名}
----------------------------------------
{正文}

----------------------------------------
第3章 {章名}
----------------------------------------
{正文}

========================================
【后续方向】
- 第4章预告：[简述]
========================================
```

### 批量模式：逐章写入正文目录

每批4章写完后，主线程执行：

1. **读取临时文件**：从 `temp_{书名}_ch{N}.txt` 读取每章正文
2. **质量检查**：
   - 字数校验：用 `python skills/story-rewrite/tools/wordcount.py` 统计
   - 禁用词扫描：对照 banned-words.md
   - 钩子检查：确认章尾有钩子
3. **写入正文目录**：`{书名}/正文/第{NNN}章_{章名}.md`
4. **删除临时文件**
5. **更新追踪文件**：
   - 追踪/伏笔.md（新增/回收伏笔）
   - 追踪/时间线.md（记录事件时序）
   - 追踪/角色状态.md（角色状态变化）
   - 追踪/上下文.md（进度摘要）

### 摘要压缩机制（批量模式专用）

**目的**：防止上下文溢出，保持连贯性。

**规则**：
- 近6章：逐章详记（事件+状态变化+伏笔）
- 更早章节：压缩为概要（每10章一段，含核心事件+角色状态变化）
- 卷级总览：每卷一句话总结

**压缩格式**：

```markdown
## 近6章详记（第{N-5}-{N}章）
第{N-5}章：{事件摘要}
第{N-4}章：{事件摘要}
...
第{N}章：{事件摘要}

## 十章概要
第1-10章 | {核心事件} | {角色状态变化}
第11-20章 | {核心事件} | {角色状态变化}
...

## 卷级总览
第一卷·{卷名} | {主线进展} | {关键转折}
```

---

## Phase 5：全书质量检查（批量模式专用）

所有章节写完后，执行全局一致性检查：

### 1. 禁用词扫描

用 grep 扫描正文目录，对照 `banned-words.md` 一级禁用词列表逐词检查。

### 2. 标题去重检查

扫描所有章节标题，发现同名或明显重复时，按本章核心事件改名。

### 3. 伏笔状态检查

检查追踪/伏笔.md，确认：
- 所有已埋伏笔状态正确
- 无断线伏笔（预计回收章节已过但未回收）
- 伏笔密度合理（每卷15-25条）

### 4. 时间线自洽检查

检查追踪/时间线.md，确认：
- 时间线连续，无跳跃
- 事件时序合理
- 无时间矛盾

### 5. 角色属性一致性检查

检查追踪/角色状态.md，确认：
- 角色属性前后一致
- 关系变化有铺垫
- 无突然的性格转变

---

## 字数要求

- 每章目标 2000 字，通过范围 1600-2400 字
- 统计方式：汉字+标点+英文字母，排除空格换行
- Agent 一次写到 1600-2400 即通过，禁止反复删减补充
- 超标（>2400）→ 主线程按段落边界截断
- 不足（<1600）→ 主线程警告用户需补充

---

## 简介要求（150-200字）

1. 主角身份+重生设定（1句）
2. 核心冲突+危机（1句）
3. 男主身份+关系（1句）
4. 悬念钩子（1句）
5. 题材标签+卖点

---

## 流程衔接

| 时机 | 跳转到 | 命令 |
|------|--------|------|
| 试水满意，正式开书 | story-long-write | `/story-long-write` |
| 想深度拆解源文本 | story-long-analyze | `/story-long-analyze` |
| 批量模式完成后 | story-review | `/story-review` 全局质量检查 |

---

## 语言

- 跟随用户的语言回复
- 中文回复遵循《中文文案排版指北》

---

## 参考文件

| 文件 | 用途 | 何时读取 |
|------|------|----------|
| `references/anti-aigc.md` | 反AIGC检测技巧 + 扫描标准 + 验证脚本 | Phase 5 全书质量检查时 |
| `references/anti-zhuque.md` | 朱雀AI检测实测记录 + Phase 5 验证脚本 | Phase 5 质量检查时参考 |
| `references/narrator-persona.md` | 人格类型定义 + 语感样本注入方式 | Phase 1 Step 2 人格匹配 + Phase 3 prompt 构造 |
| `references/踩坑记录.md` | 历史问题与解法 | 排查 bug 时参考 |
| `references/success-patterns.md` | 仿写成功模式 | Phase 1 框架生成时 |
| `references/structure-extraction.md` | 结构提取方法 | Phase 1 读源文本时 |
